#include <iostream>
#include <cmath>

int main (){

int x = -5;
int y = 4;
int z;
double m = 3.14;


//z = std::max(x, y);
//z = std::min(x, y);
//z = pow(3, 2);
//z = pow(x, y );

//z = sqrt(y);

//z = abs(x);

//z = round(m);
//z = ceil(m);
//z = floor(m);


std::cout << z << '\n';

    return 0;
}